<header class="header-area">
    <!--<div class="left">
        <a href="Javascript:void(0)"><i class="fa fa-search" aria-hidden="true"></i></a>
    </div>
    <div class="right">
        <a href="Javascript:void(0)"><i class="fa fa-user" aria-hidden="true"></i></a>
    </div>-->
    <div class="container-fluid">
        <div class="row">
            <div class="col-sm-3 logo_sm">
                <div class="logo">
                    <a href="index.html"></a>
                </div>
            </div>
            <div class="col-lg-12 col-md-12 col-sm-9">
                    <nav class="site-navbar">
                        <ul>
                            <li><a {% if pageTitle == "home" %}class="active"{% endif %} href="{{ path('app_home') }}">Accueil</a></li>
                            <li><a {% if pageTitle == "service" %}class="active"{% endif %} href="{{ path('app_service') }}">Services</a></li>
                            <li><a href="index.html">L'agence</a></li>
                            <li><a {% if pageTitle == "appointment" %}class="active"{% endif %} href="{{ path('app_appointment') }}">Rendez-vous</a></li>
                            <li><a {% if pageTitle == "contact" %}class="active"{% endif %} href="{{ path('app_contact') }}">Contact</a></li>
                            {% if is_granted('ROLE_USER')%}
                                {#<li><a href="{{ path('app_logout') }}">Déconnexion</a></li>#}
                            {% else %}
                                {#<li><a href="{{ path('app_login') }}">Connexion</a></li>#}
                            {% endif %}
                        </ul>
                        <button class="nav-toggler">
                            <span></span>
                        </button>
                    </nav>
            </div>
        </div>
    </div>
</header>