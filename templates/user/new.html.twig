{% extends 'base.html.twig' %}

{% block title %}New User{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-lg-12 center">
            <div class="card mb-4">
                <div class="card-body text-center">
                    <h1>Créer un utilisateur</h1>
                </div>
            </div>
        </div>
    </div>
    <div class="row mb-3">
        <div class="col-md-2">
            <a href="{{ path('app_user_index') }}" class="btn btn-primary btn-user btn-block">< Retour</a>
        </div>
    </div>
    <div class="row">
        <div class="col-md-6">
            <div class="alert alert-info">
                Les champs avec des étoiles rouges sont obligatoires.<br>
                L'identifiant et le mot de passe seront envoyé automatiquement par e-mail lors de la création de l'utilisateur.
            </div>
        </div>
        <div class="card col-md-6 p-3">
            {{ include('user/_form.html.twig') }}
        </div>
    </div>
{% endblock %}
